apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: storpvc
spec:
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: sc
