apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: storpvc
spec:
  volumeName: stor
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
